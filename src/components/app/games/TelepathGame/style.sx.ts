import type { SxProps } from '@mui/material'

export const container: SxProps = {
  alignContent: 'center',
  alignItems: 'center',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
}

export const questionText: SxProps = {
  mb: 2,
}

export const wordForm: SxProps = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  maxWidth: 400,
  width: '100%',
}

export const formControl: SxProps = {
  mb: 2,
}
